[
	{
		"name": "ImageMagick",
		"description": "使用 ImageMagick WASM 转换图片格式。支持参数：#format=avif|webp|jpeg|png（默认 avif），#quality=1-100（默认 80），#minSavings=0-100（默认 10，转换后节省低于该百分比时保留原文件）",
		"scriptURL": "preprocess-scripts/imagemagick.json#format=avif&quality=80"
	}
]
